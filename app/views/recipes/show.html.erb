<!DOCTYPE html>
<html>
<head>
  <title>Your Recipe</title>
</head>
<body>
  <div class="banner d-flex justify-content-center h-40" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= @recipe.image_url %>);">
    <div class="container">
      <h1 class="text-center"> <strong><%= @recipe.name.html_safe %></strong></h1>
    </div>
    <a class="btn btn-primary" id="btn-lg" href='/meal_plans/new'>Back to meal plan</a>
  </div>
  <div class="m-2 justify-content-center">
    <div class="border p-4 mt-5 justify-content-center">
      <div class="row mb-3">
        <div class="col-2"></div>
        <a class="col-8 border-bottom text-center font-c-3" data-toggle="collapse" href="#collapseIngredients" role="button"><strong>Ingredients</strong></a>
        <div class="col-2"></div>
      </div>
      <p class="text-center" id="collapseIngredients"> <% @all_recipe_ingredients.each do |item| %>
          <% number = item.display_amount  %>
          <%= number == number.floor ? item.display_amount.round(0) : item.display_amount.round(2) %>
          <%= item.display_unit %>
          <%= item.ingredient.name  %>
          <br>
          <% end%>
      </p>
      <div class="row mb-3 mt-5">
        <div class="col-2"></div>
        <div class="col-8 border-bottom text-center font-c-3" data-toggle="collapse" href="#collapseDescription" role="button"><strong>Summary</strong></div>
        <div class="col-2"></div>
      </div>
      <p class="text-center" id="collapseDescription"> <%= @recipe.description.html_safe %> </p>
      <div class="row mb-3 mt-5">
        <div class="col-2"></div>
        <div class="col-8 border-bottom text-center font-c-3" data-toggle="collapse" href="#collapseInstruction" role="button"><strong>Instructions</strong></div>
        <div class="col-2"></div>
      </div>
      <p class="text-center" id="collapseInstruction"> <%= @recipe.instructions.html_safe %> </p>
    </div>
  </div>
</body>
</html>
