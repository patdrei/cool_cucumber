<div class="logo">
  <div class="quarter-ellipse3">
    <%= link_to "/", class: "logo-brand" do %>
    <%= image_tag "logo.svg"  %>
    <% end %>
      <% @plan = MealPlan.find_by(user: current_user, active: true) %>
  </div>

    <!-- <i class="fas fa-search icon mr-4 m-2"></i> -->
    <% if user_signed_in? %>

    <% else %>
      <form class="form-inline">
        <div class="login">
                <%= link_to new_user_session_path, { class: "btn btn-success btn-outline-black mt-2 mr-2",  style: "text-decoration:none;" } do %>
          <span>Login</span>
       <% end %>
        </div>
      </form>
   <% end %>

</div>

